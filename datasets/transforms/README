1) apply stylesheet for each file: xmlstarlet tr XSLT_FILE XML_FILE_INPUT 
   scripts/apply_single_stylesheets.sh
2) concat (removing header for each file)
3) apply global stylesheet
4) convert xml to json_flat and then to csv with java
1,2,3,4 with "generate_total"

5) load csv to spreadsheet
6) copy address column from "geocoding" sheet
7) get geocoding using "geocoding.sh"
8) populate geocoding column with these new data

9) sync to fusiontable
